<li>
	<div class="panel panel-default">
		<div class="panel-body">
			<% if !query.blank? %>
				<%= raw fact.content.sub(query, "<mark>#{query}</mark>") %>
			<% else %>
				<%= fact.content %>
			<% end %>
		</div>
	</div>
</li>
