$searchResults = $("#search-results .search-result");

if ($searchResults.length == 0){
	$("#search-results").html("<%= escape_javascript(render(@fact)) %>");
}else{
	$searchResults.first().before("<%= escape_javascript(render(@fact)) %>");
}

$searchResults.first().hide().slideDown('fast');